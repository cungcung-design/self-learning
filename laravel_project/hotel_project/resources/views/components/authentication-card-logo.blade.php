<a href="{{ route('home.public') }}" class="flex flex-col items-center">
    <img src="{{ asset('images/logo.png') }}" alt="{{ config('hotel.name') }}" class="h-12 w-auto">
    <span class="mt-3 text-sm font-semibold tracking-wide text-gray-700">{{ config('hotel.name') }}</span>
</a>
