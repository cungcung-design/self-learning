# TODO

- [x] Fix Vue runtime crash in `src/components/SignUp.vue` (`ReferenceError: user is not defined`) by declaring `user` in `mounted()`.
- [x] Verify the app no longer throws the mounted-hook error.

- [ ] If signup still fails, ensure backend is running on `http://localhost:3000` (e.g., json-server).
